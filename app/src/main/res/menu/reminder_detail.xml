<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_reminder_edit"
        android:icon="@android:drawable/ic_menu_edit"
        android:title="@string/menu_reminder_edit"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/menu_reminder_delete"
        android:icon="@android:drawable/ic_menu_delete"
        android:title="@string/menu_reminder_delete"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/menu_reminder_save"
        android:icon="@android:drawable/ic_menu_save"
        android:title="@string/menu_reminder_save"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/menu_reminder_cancel"
        android:icon="@android:drawable/ic_menu_close_clear_cancel"
        android:title="@string/menu_reminder_cancel"
        app:showAsAction="ifRoom" />

</menu>

